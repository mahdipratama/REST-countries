import CountryList from '../components/CountryList';
import SearchBar from '../components/SearchBar';

const Home = () => {
  return (
    <main>
      <SearchBar />
      <CountryList />
    </main>
  );
};

export default Home;
